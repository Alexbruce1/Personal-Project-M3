export const setData = (media) => ({
  type: 'SET_DATA',
  media
});

export const addQuery = (query) => ({
  type: 'ADD_QUERY',
  query
});

export const addDataType = (dataType) => ({
  type: 'ADD_DATA_TYPE',
  dataType
});
